services:
  anyhaven:
    build: .
    user: "${UID:-1000}:${GID:-1000}"
    ports:
      - "3000:3000"
    volumes:
      - ./config:/config
    environment:
      - CONFIG_PATH=/config/settings.json
    network_mode: host
